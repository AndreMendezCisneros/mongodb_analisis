# Versión mínima sin scikit-learn ni numpy (solo requiere compilación básica)
# Esta versión funciona sin necesidad de Microsoft Visual C++ Build Tools

flask>=3.0.0
flask-cors>=4.0.0
pymongo>=4.6.0

# pysentimiento para análisis de sentimientos preciso (recomendado)
# Si no se instala, el código usará análisis manual basado en palabras clave
pysentimiento>=0.6.2
torch>=1.9.0
transformers>=4.20.0

# Nota: scikit-learn y numpy son opcionales
# El código funcionará sin ellos usando implementaciones manuales

